@model IEnumerable<EShop.Models.PaymentMode>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PaymentModeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PaymentModeIsActive)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PaymentModeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PaymentModeIsActive)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PaymentModeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PaymentModeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PaymentModeID })
        </td>
    </tr>
}

</table>
